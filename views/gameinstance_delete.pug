extends layout

block content
    h1= title
    p #[strong Are you sure you want to delete this copy?]

    form(method="POST")
        div.form-group
            input#gameinstanceid.form-control(type='hidden', name='id', value=gameinstance._id )

        button.btn.btn-primary(type='submit') Delete